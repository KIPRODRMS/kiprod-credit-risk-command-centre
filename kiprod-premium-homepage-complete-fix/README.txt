KIPROD Command Centre - Premium Homepage Complete Fix

Copy the included app folder into the project root and replace both files.
Then restart the development server and hard-refresh the browser.

This package keeps page.tsx and globals.css on the same verified version.
